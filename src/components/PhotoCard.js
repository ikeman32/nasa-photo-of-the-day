import React from "react";

const PhotoCard = props => {
    return ( <
        div >
        <
        img src = { props.url }
        alt = "" / > < /
        div >
    );
};

export default PhotoCard;